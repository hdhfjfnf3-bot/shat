// server.cjs — NOT USED ANYMORE
// Auth is handled entirely via Supabase RPC functions.
// For production, Vercel Serverless Functions in /api/auth/ are used.
// For local development, the frontend calls Supabase directly — no local server needed.
//
// To start the app locally, just run:
//   pnpm run dev
//
// To deploy, push to your Vercel project.
