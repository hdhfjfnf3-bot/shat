kind = "web"
previewPath = "/"
title = "Direct"
version = "1.0.0"
id = "artifacts/insta-chat"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22594

[services.development]
run = "pnpm --filter @workspace/insta-chat run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/insta-chat", "run", "build" ]
publicDir = "artifacts/insta-chat/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22594"
BASE_PATH = "/"
